<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <!-- This tool is a throwaway harness for validating the existing .NET
         AcmeClient against a local Pebble CA. It is not part of the shipped
         solution and intentionally excluded from Acmebot.slnx. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Acmebot.Acme\Acmebot.Acme.csproj" />
  </ItemGroup>

</Project>
