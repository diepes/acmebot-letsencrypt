
# Environment variables
.env
.env.acmebot

# Local dev tooling (downloaded binaries, not committed)
.tools/

# docker-compose local test output (issued certs, see docker-compose.yml)
/out/

# Rust (rust/acmebot-acme)
/rust/target/
**/*.rs.bk

# Output from the Pebble parity harnesses (tools/pebble-parity, rust/acmebot-acme examples)
*.dotnet.pem
*.rust.pem

# Core dumps
core
core.*

# VS Code settings
.vscode/
