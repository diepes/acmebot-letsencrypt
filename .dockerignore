# Only the Rust workspace is needed to build the acmebot container image.
# Everything else (legacy C# app, docs, tests, tooling) is excluded from the
# build context to keep builds fast and avoid leaking unrelated files.
*
!rust/
rust/target/
